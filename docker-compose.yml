services:
  backend:
    build: ./mern-demo/server
    container_name: mern-backend
    ports:
      - "5000:5000"
    dns:
      - 8.8.8.8
      - 8.8.4.4
    environment:
      - PORT=5000
      - MONGODB_URI=mongodb+srv://huong:huong123@cluster0.jdahtd2.mongodb.net/cloud_lab?retryWrites=true&w=majority
  frontend:
    build: ./mern-demo/client
    container_name: mern-frontend
    ports:
      - "3000:3000"
    dns:
      - 8.8.8.8
      - 8.8.4.4
    depends_on:
      - backend
